- name: Make sure srv dir exists
  file: 
    path: "{{ sparkworker_srv_dir }}/" 
    state: directory

- name: Make sure config dir exists
  file: 
    path: "{{ sparkworker_config_dir }}/" 
    state: directory 

- name: Make sure log dir exists
  file: 
    path: "{{ sparkworker_log_dir }}/"
    state: directory 
